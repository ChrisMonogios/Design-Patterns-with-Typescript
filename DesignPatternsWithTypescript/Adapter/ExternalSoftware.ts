module adapter {
    export class ExternalSoftware implements IExternalSoftware {
        method1 = (): void => {

        }

        method2 = (param: string): void => {

        }

        method3 = (param: number, param2: string): string => {
            return "";
        }

        method4 = (param: number): number => {
            return 1;
        }
    }
}